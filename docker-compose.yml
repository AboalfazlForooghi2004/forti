version: "3.9"

services:
  backend:
    build: ./backend
    container_name: fortigate_backend
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: always

  frontend:
    build: ./frontend
    container_name: fortigate_frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: always
